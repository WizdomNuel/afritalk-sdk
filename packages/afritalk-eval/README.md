# AfriTalk Eval

Lightweight evaluation harness for African languages. Produces JSON output and supports provider selection via environment variables or CLI args.

Usage:

```
node -r ts-node/register src/cli.ts swahili google
```
